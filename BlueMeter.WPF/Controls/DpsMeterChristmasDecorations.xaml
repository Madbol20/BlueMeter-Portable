<UserControl
    x:Class="BlueMeter.WPF.Controls.DpsMeterChristmasDecorations"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="400"
    d:DesignWidth="400"
    IsHitTestVisible="False"
    mc:Ignorable="d">

    <UserControl.Resources>
        <!-- Sparkle animation -->
        <Storyboard x:Key="SparkleAnimation" RepeatBehavior="Forever">
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="Opacity">
                <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0" />
                <LinearDoubleKeyFrame KeyTime="0:0:0.3" Value="1" />
                <LinearDoubleKeyFrame KeyTime="0:0:0.6" Value="0" />
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
                <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0.5" />
                <LinearDoubleKeyFrame KeyTime="0:0:0.3" Value="1.5" />
                <LinearDoubleKeyFrame KeyTime="0:0:0.6" Value="0.5" />
            </DoubleAnimationUsingKeyFrames>
            <DoubleAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
                <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0.5" />
                <LinearDoubleKeyFrame KeyTime="0:0:0.3" Value="1.5" />
                <LinearDoubleKeyFrame KeyTime="0:0:0.6" Value="0.5" />
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
    </UserControl.Resources>

    <Grid>
        <!-- Festive Background Layer (behind everything, doesn't replace user background) -->
        <Image
            Opacity="0.15"
            Panel.ZIndex="0"
            Source="pack://application:,,,/Assets/Themes/Christmas/Backgrounds/festive_background.jpg"
            Stretch="UniformToFill" />

        <!-- Subtle snow texture overlay -->
        <Image
            Opacity="0.08"
            Panel.ZIndex="1"
            Source="pack://application:,,,/Assets/Themes/Christmas/Backgrounds/snow_texture.png"
            Stretch="UniformToFill" />

        <!-- Frost Border Frame -->
        <Border
            Panel.ZIndex="999"
            BorderThickness="0"
            CornerRadius="{StaticResource MainCornerRadius}">
            <Border.Background>
                <ImageBrush
                    ImageSource="pack://application:,,,/Assets/Themes/Christmas/Backgrounds/frost_border.png.png"
                    Opacity="0.6"
                    Stretch="Fill" />
            </Border.Background>
        </Border>

        <!-- Sparkle Effects Canvas -->
        <Canvas x:Name="SparkleCanvas" Panel.ZIndex="50" ClipToBounds="True" />
    </Grid>
</UserControl>
